# Copy to .env.local after running: vercel env pull .env.local
PGHOST=
PGPORT=5432
PGUSER=
PGDATABASE=postgres
AWS_REGION=
AWS_ROLE_ARN=
VERCEL_OIDC_TOKEN=

# Bedrock — Nova Lite for screenshot extraction
BEDROCK_MODEL_ID=us.amazon.nova-lite-v1:0
